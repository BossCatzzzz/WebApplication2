@model IEnumerable<WebApplication2.Models.LOAISANPHAM>

@{
    ViewBag.Title = "Loại sản phẩm";
}

<h2>Danh sách các loại sản phẩm</h2>

<p>
    @Html.ActionLink("Thêm loại mới", "ThemLoaiSP")
</p>
<table class="table">
    <tr>
        @*<th>
            @Html.DisplayNameFor(model => model.ID)
        </th>*@
        <th>
            Mã loại
        </th>
        <th>
            Tên loại
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    @*<th>
        @Html.DisplayFor(modelitem => item.ID)
    </th>*@
    <td>
        @Html.DisplayFor(modelItem => item.MALOAI)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.TENLOAI)
    </td>
    <td>
        @Html.ActionLink("Sửa", "Edit", new { id = item.ID }) |
        @Html.ActionLink("Chi tiết", "Details", new { id = item.ID }) |
        @Html.ActionLink("Xóa", "Delete", new { id = item.ID })   |  
        @Html.ActionLink("Xem sản phẩm","SanPhams",new {IDloaisp = item.ID })
    </td>
</tr>
}

</table>
